package algorithm

import "fmt"

// import (
// 	"time"
// )

func MergeSort(arr []int, lowerbound int, uperbound int ) ([]int, ) {
fmt.Printf("%v %v %v\n", arr, lowerbound, uperbound)

	// for i := 1; i < len(arr); i++ {
	// 	j := i
	// 	for j > 0 {
	// 		if arr[j-1] > arr[j] {
	// 			arr[j], arr[j-1] = arr[j-1], arr[j]
	// 		}
	// 		j--
	// 	}

	// }
	
	return arr
}
